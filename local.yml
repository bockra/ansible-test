---
- hosts: 127.0.0.1
  remote_user: bockra
  become: true
  tasks:  
  - name: Install mc
    apt:
      name: mc 
      state: present
      force: yes
      update_cache: yes
      install_recommends: yes
#  - name: start nginx
#    service: 
#      name: nginx
#      state: started
